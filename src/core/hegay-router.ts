// src/core/hegay-router.ts

export async function hegayRouter(message: string): Promise<string> {
  // Temporary placeholder logic
  // Replace with your real AI pipeline later
  return `Hegay AI reply to: ${message}`;
}
